apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aces-ml-chart.fullname" . }}-test-connection"
  labels:
    {{- include "aces-ml-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aces-ml-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
