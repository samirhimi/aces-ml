apiVersion: apps/v1
kind: Deployment
metadata:
  name: aces-ml
  namespace: aces-ml
  labels:
    app: aces-ml
spec:
  replicas: 1
  selector:
    matchLabels:
      app: aces-ml
  template:
    metadata:
      labels:
        app: aces-ml
    spec:
      containers:
        - name: aces-ml
          image: sami4rhimi/aces-ml:1.0.4
          imagePullPolicy: Always
          command: ["flask"]
          args: ["run", "--host=0.0.0.0", "--port=8080"]
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          resources:
            limits:
              cpu: "1000m"
              memory: "2Gi"
            requests:
              cpu: "500m"
              memory: "1Gi"
          volumeMounts:
            - name: model-volume
              mountPath: /app/models
            - name: dataset-volume
              mountPath: /app/data
            - name: tmp-volume
              mountPath: /tmp
              readOnly: false
          env:
            - name: ML_MODEL_PATH
              value: "/app/models/random_forest_model.joblib"
            - name: LOG_LEVEL
              value: "INFO"
            - name: FLASK_ENV
              value: "development"
            - name: FLASK_APP
              value: "/app/ML.py"
            - name: FLASK_RUN_PORT
              value: "8080"
      volumes:
        - name: model-volume
          persistentVolumeClaim:
            claimName: aces-aces-ml-model
        - name: dataset-volume
          persistentVolumeClaim:
            claimName: aces-aces-ml-dataset
        - name: tmp-volume
          emptyDir:
            medium: Memory
            sizeLimit: 512Mi